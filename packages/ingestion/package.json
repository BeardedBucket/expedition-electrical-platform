{
  "name": "@expedition/ingestion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b tsconfig.json --force",
    "test": "vitest run",
    "ingest:pilot": "npm run build && node scripts/victron-multiplus-pilot.mjs",
    "replay:pilot": "npm run build && node scripts/victron-multiplus-pilot.mjs --replay"
  },
  "dependencies": {
    "parse5": "8.0.1"
  }
}
